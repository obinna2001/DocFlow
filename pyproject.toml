[project]
name = "docflow"
version = "0.1.0"
description = "A document translation app powered by models"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "pydantic>=2.13.4",
    "ruff>=0.15.12",
]
